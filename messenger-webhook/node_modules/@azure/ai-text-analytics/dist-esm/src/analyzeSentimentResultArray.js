// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeAnalyzeSentimentResult, makeAnalyzeSentimentErrorResult } from "./analyzeSentimentResult";
import { sortResponseIdObjects } from "./util";
export function makeAnalyzeSentimentResultArray(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeAnalyzeSentimentResult(document.id, document.sentiment, document.confidenceScores, document.sentenceSentiments, document.warnings, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeAnalyzeSentimentErrorResult(error.id, error.error);
    }));
    const result = sortResponseIdObjects(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=analyzeSentimentResultArray.js.map